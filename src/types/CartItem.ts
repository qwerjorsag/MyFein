export type CartItem = {
    id: string;
    name: string;
    selectedWeight: string;
    quantity: number;
};

// export const filteredCartItems = cartItems.map(({ id, name, selectedWeight, quantity }) => ({
//     id,
//     name,
//     selectedWeight,
//     quantity,
// }));
