// src/theme/breakpoints.ts
const breakpoints = {
    values: {
        xs: 0, // Extra small screens
        sm: 600, // Small screens (phones)
        md: 960, // Medium screens (tablets)
        lg: 1280, // Large screens (laptops)
        xl: 1920, // Extra large screens (desktops)
    },
};

export default breakpoints;
